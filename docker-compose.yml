version: "3.8"

services:
  redis:
    image: redis:alpine
    container_name: geoai-redis
    ports:
      - "6379:6379"
    volumes:
      - geoai_redis_data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped

volumes:
  geoai_redis_data:
