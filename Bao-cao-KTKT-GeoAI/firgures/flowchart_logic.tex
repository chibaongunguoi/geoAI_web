\begin{tikzpicture}[
  scale=0.85,
    transform shape,
    table/.style={
        draw,
        rectangle split,
        rectangle split parts=2,
        rectangle split part fill={white, white},
        every text node part/.style={align=center, font=\bfseries},
        every two node part/.style={align=left},
        minimum width=4.5cm,
        text width=4cm
    },
    % Style riêng cho các bảng bên phải (rộng hơn)
    tableright/.style={
        draw,
        rectangle split,
        rectangle split parts=2,
        rectangle split part fill={white, white},
        every text node part/.style={align=center, font=\bfseries},
        every two node part/.style={align=left},
        minimum width=6cm,
        text width=5.5cm
    },
    % Cấu hình chung cho mũi tên (đặt 1 lần duy nhất)
    >=Stealth,
    ->,
    thick,
    rounded corners=2pt
]

% ================= Các Nút (Bảng) =================

% --- Cột Giữa ---
\node[table] (assets) at (0, 2) {
    Assets
    \nodepart{two}
    Asset\_id \\
    District\_id \\
    Type\_id \\
    Name \\
    Geom \\
    Address \\
    Status \\
    Created\_at \\
    Updated\_at
};

\node[table] (users) at (0, -6.5) {
    Users
    \nodepart{two}
    \underline{\textbf{User\_id}} \\
    Role\_id \\
    Username \\
    Password \\
    Name \\
    Email \\
    Phone \\
    Status \\
    Created\_at
};

% --- Cột Trái ---
\node[tableright] (history) at (-6.5, 7.5) {
    Asset\_status\_history
    \nodepart{two}
    \underline{\textbf{History\_id}} \\
    Asset\_id \\
    Changed\_by \\
    Status\_old \\
    Status\_new \\
    Change\_reason \\
    Changed\_at \\
    Note
};

\node[tableright] (orders) at (-6.5, 1) {
    Maintenance\_orders
    \nodepart{two}
    \underline{\textbf{Order\_id}} \\
    Asset\_id \\
    Type \\
    Priority \\
    Title \\
    Description \\
    Due\_date \\
    Status \\
    Created\_at
};

\node[table] (reports) at (-6.5, -5.5) {
    Citizen\_reports
    \nodepart{two}
    \underline{\textbf{Report\_id}} \\
    Asset\_id \\
    User\_id \\
    Description \\
    Photo\_keys \\
    Status \\
    Created\_at
};

% --- Cột Phải (Đã thu hẹp về bên trái 1 đoạn tại x = 5.5) ---
\node[table] (districts) at (5.5, 6.5) {
    Districts
    \nodepart{two}
    \underline{\textbf{District\_id}} \\
    Code \\
    Name \\
    Geom \\
    Acreage \\
    Population
};

\node[table] (assettypes) at (5.5, 0.5) {
    Asset\_types
    \nodepart{two}
    \underline{\textbf{Type\_id}} \\
    Parent\_type\_id \\
    Name \\
    Description \\
    Created\_at \\
    Updated\_at
};

\node[table] (roles) at (5.5, -6.5) {
    Roles\_permissions
    \nodepart{two}
    \underline{\textbf{Role\_id}} \\
    Name \\
    Description \\
    Permissions \\
    Created\_at
};

% ================= Các Mối Quan Hệ (Mũi tên) =================

% Trái nối vào Giữa (Assets)
% Đặt đường gấp khúc chung ở tọa độ x = -3.5 để các đường chạy song song gọn gàng
\draw (history.east) -- (-3.0, 7.5) |- ([yshift=1cm]assets.west);
\draw (orders.east) -- (-3.0, 1) |- (assets.west);
\draw ([yshift=0.5cm]reports.east) -- (-3.0, -5) |- ([yshift=-1cm]assets.west);

% Trái nối vào Giữa (Users)
\draw ([yshift=-1cm]reports.east) -- (-3.0, -6.5) |- ([yshift=1cm]users.west);

% Giữa nối ra Phải
% Đặt đường gấp khúc chung ở tọa độ x = 2.8
\draw ([yshift=1cm]assets.east) -- (2.8, 3) |- (districts.west);
\draw ([yshift=-1cm]assets.east) -- (2.8, 1) |- (assettypes.west);

% Giữa (Users) nối ra Phải (Roles)
\draw ([yshift=1cm]users.east) -- (2.8, -5.5) |- ([yshift=1cm]roles.west);

\end{tikzpicture}