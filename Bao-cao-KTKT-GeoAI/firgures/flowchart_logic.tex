\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    scale=0.68,
    transform shape,
    table/.style={
        draw,
        rectangle split,
        rectangle split parts=2,
        rectangle split part fill={gray!10, white},
        every text node part/.style={align=center, font=\bfseries},
        every two node part/.style={align=left, font=\scriptsize},
        minimum width=3.8cm,
        text width=3.8cm,
        rounded corners=2pt
    },
    longtable/.style={
        draw,
        rectangle split,
        rectangle split parts=2,
        rectangle split part fill={gray!10, white},
        every text node part/.style={align=center, font=\bfseries},
        every two node part/.style={align=left, font=\scriptsize},
        minimum width=5.3cm,
        text width=5.4cm,
        rounded corners=2pt
    },
    rel/.style={-Stealth, thick},
    spatial/.style={Stealth-Stealth, thick, dashed},
    note/.style={font=\scriptsize, align=center, fill=white, inner sep=1pt}
]

% ================= Người dùng, phân quyền, phiên =================
\node[table] (user) at (-8, 4.5) {
    User
    \nodepart{two}
    \underline{\textbf{id}}\\
    username\\
    email\\
    passwordHash\\
    status
};

\node[table] (session) at (-13, 7.5) {
    Session
    \nodepart{two}
    \underline{\textbf{id}}\\
    userId (FK)\\
    refreshToken\\
    expiresAt\\
    revokedAt
};

\node[table] (layerconfig) at (-13, 4.5) {
    LayerUserConfig
    \nodepart{two}
    \underline{\textbf{userId}}\\
    state\\
    createdAt\\
    updatedAt
};

\node[longtable] (displayconfig) at (-13, 1.5) {
    AssetDisplayUserConfig
    \nodepart{two}
    \underline{\textbf{userId}}\\
    state\\
    createdAt\\
    updatedAt
};

\node[table] (userrole) at (-8, 7.5) {
    UserRole
    \nodepart{two}
    \underline{\textbf{userId}} (FK)\\
    \underline{\textbf{roleId}} (FK)
};

\node[table] (role) at (-3, 7.5) {
    Role
    \nodepart{two}
    \underline{\textbf{id}}\\
    code\\
    name\\
    description
};

\node[table] (rolepermission) at (2, 7.5) {
    RolePermission
    \nodepart{two}
    \underline{\textbf{roleId}} (FK)\\
    \underline{\textbf{permissionId}} (FK)
};

\node[table] (permission) at (8, 7.5) {
    Permission
    \nodepart{two}
    \underline{\textbf{id}}\\
    key\\
    group\\
    name
};

\node[table] (audit) at (-3, 4.5) {
    AuditLog
    \nodepart{two}
    \underline{\textbf{id}}\\
    actorUserId (FK)\\
    action\\
    entityType\\
    metadata
};

% ================= Tài sản, tiện ích, phản ánh =================
\node[table] (building) at (0, 1.0) {
    BuildingProperty
    \nodepart{two}
    \underline{\textbf{id}}\\
    code, overtureId\\
    geom (Geometry)\\
    searchTextNormalized\\
    embedding\\
    riskFlags
};

\node[table] (place) at (6, 1.0) {
    Place
    \nodepart{two}
    \underline{\textbf{id}}\\
    overtureId\\
    category, name\\
    location (Point)\\
    confidence\\
    riskFlags
};

% Đã dời Report vào chính giữa bên dưới User để nối lên đẹp nhất
\node[table] (report) at (-8, -0.5) {
    Report
    \nodepart{two}
    \underline{\textbf{id}}\\
    userId (FK)\\
    reason\\
    message\\
    latitude, longitude\\
    status
};

\node[table] (notification) at (-3, -2.5) {
    Notification
    \nodepart{two}
    \underline{\textbf{id}}\\
    userId (FK)\\
    title\\
    message\\
    type\\
    isRead
};

% ================= Không gian và rủi ro =================
\node[table] (riskzone) at (12, 4.5) {
    RiskZone
    \nodepart{two}
    \underline{\textbf{id}}\\
    zoneType\\
    riskLevel\\
    source\\
    geom (Polygon)
};

\node[table] (road) at (12, -2.5) {
    RoadSegment
    \nodepart{two}
    \underline{\textbf{id}}\\
    osmId\\
    name\\
    highway\\
    geom (LineString)
};

% ================= QUAN HỆ KHÓA NGOẠI (Căn điểm neo chính xác 100%) =================

% Từ Cột trái dồn vào viền Tây (.west) của User
% Đi từ cạnh phải Session, bẻ góc vuông vòng xuống điểm cách User 0.3cm, rồi đâm ngang vào User
\draw[rel] (session.east) -| ([xshift=-0.3cm, yshift=1.2cm]user.west) -- ([yshift=1.2cm]user.west);

% Bảng ở giữa đi thẳng trực tiếp không vướng
\draw[rel] (layerconfig.east) -- (user.west);

% Đi từ cạnh phải AssetDisplay, bẻ góc vuông vòng lên điểm cách User 0.3cm, rồi đâm ngang vào User
\draw[rel] (displayconfig.east) -| (user.south) ;
% Từ cột dọc và ngang (đi thẳng trực tiếp không vướng)
\draw[rel] (user.north) -- (userrole.south);
\draw[rel] (userrole.east) -- (role.west);
\draw[rel] (role.east) -- (rolepermission.west);
\draw[rel] (rolepermission.east) -- (permission.west);
\draw[rel] (audit.west) -- (user.east);
\draw[rel] (report.north) -- (user.south);

% Notification đi từ đỉnh (north), vòng lên một đoạn an toàn, rồi đâm ngang vào nửa dưới viền Đông (.east) của User
\draw[rel] (notification.north) -- ++(0, 0.0) -/ (user.south);

% ================= QUAN HỆ LOGIC KHÔNG GIAN =================

% 1. Nối BuildingProperty và Place (Đi thẳng)
\draw[spatial] (building.east) -- (place.west) node[note, midway, above] {ST\_DWithin};

% 2. BuildingProperty -> RiskZone (Vọt cao lên ngút tầm mắt Y=7.0 để né rồi mới bẻ ngang xuống RiskZone)
% (Ghi chú đặt ở pos=0.25 tức là nằm chễm chệ trên đoạn thẳng nằm ngang)
\draw[spatial] (building.north) -- ++(0, 2.0) -/ (riskzone.west) ;
% 3. BuildingProperty -> RoadSegment (Cắm thẳng xuống Y=-4.5 rồi mới bẻ ngang qua RoadSegment)
\draw[spatial] (building.south) -- (building.south |- 0, -4.5) -| (road.south) node[note, pos=0.25, below] {ST\_Distance};

% 4. Place -> RiskZone và Road (Léo ngang qua phải 1 đoạn rồi mới bẻ lên/xuống)
\draw[spatial] (place.east) -- ++(1.2,0) |- (riskzone.west) node[note, near start, above] {risk overlay};
\draw[spatial] (place.east) -- ++(1.2,0) |- (road.west) node[note, near start, below] {proximity};

\end{tikzpicture}%
}